{
  "name": "binance-p2p-cron-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker con Cron Trigger para monitorear Binance P2P cada 60 segundos",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240903.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.75.0"
  }
}
